from setuptools import setup, find_packages

setup(
    name="dbmux",
    version="1.0.0",
    description="Polyglot DBMux Python SDK with capability bitmask discovery.",
    packages=find_packages(),
    python_requires=">=3.8",
)
